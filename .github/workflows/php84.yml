name: PHP 8.4

on:
  workflow_dispatch:
  schedule: # Run monthly
    - cron: "14 3 20 * *"

permissions:
  contents: read

jobs:
  build:

    runs-on: ${{ matrix.os }}
    env:
      WEBPCONVERT_EWWW_API_KEY: ${{ secrets.WEBPCONVERT_EWWW_API_KEY }}

    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-26.04, ubuntu-24.04, windows-2025, windows-2022, macos-26, macos-14]
        php: [8.4]

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ matrix.php }}
        coverage: none
        extensions: exif, mbstring, fileinfo, gd, vips, imagick, gmagick

    - name: Validate composer.json
      run: composer validate --strict

    - name: Composer install
      run: composer install --prefer-dist --no-progress

    - name: Run phpunit (test cases)
      run: composer run-script test

    - name: Run phpstan
      run: composer run-script phpstan

    - name: run phpcs (to check coding style)
      run: composer run-script phpcs-all
